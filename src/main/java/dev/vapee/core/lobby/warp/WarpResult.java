package dev.vapee.core.lobby.warp;

public enum WarpResult {
    SUCCESS,
    INVALID_ID,
    INVALID_NAME,
    INVALID_ICON,
    NOT_FOUND,
    PLAYER_OFFLINE,
    WORLD_NOT_LOADED,
    TELEPORT_FAILED
}
